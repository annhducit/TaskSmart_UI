import LoadingPage from './page';
import LoadingSekeleton from './skeleton';
import LoadingSpinner from './spinner';
import LoadingText from './text';

const Loading = {
  Text: LoadingText,
  Spinner: LoadingSpinner,
  Page: LoadingPage,
  Skeleton: LoadingSekeleton,
};

export default Loading;
