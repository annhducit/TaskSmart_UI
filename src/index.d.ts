/**
 * @description Import here so that it can be used in other files without having to import it again
 * @author Duc Nguyen
 */
interface TSMNote {
  id: number;
  title: string;
  content: string;
  createdAt: string;
}
